services:
  rclone-sync:
    image: ghcr.io/你的GitHub用户名/rclone-sync:latest
    container_name: rclone-sync
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /path/to/your/media:/data
      - /path/to/config:/config
    environment:
      - TZ=Asia/Shanghai
